<%= simple_form_for([@picturable, @picture]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.file_field :photo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>